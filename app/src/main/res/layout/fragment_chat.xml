<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:context=".GroupActivityClasses.ChatFragmentClasses.ChatFragment">

    <LinearLayout
        android:id="@+id/linear1"
        android:orientation="horizontal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <Button
            android:id="@+id/manageGrpBtn"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="wrap_content"
            android:text="Manage groups"/>
        <Button
            android:id="@+id/mapsBtn"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="wrap_content"
            android:text="Map view"/>

    </LinearLayout>

    <!--For messages-->
    <FrameLayout
        android:id="@+id/messageRecyclerViewContainer"
        android:layout_width="match_parent"
        android:layout_height="120dp"
        app:layout_constraintTop_toBottomOf="@id/linear1"
        app:layout_constraintStart_toStartOf="parent"/>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@id/messageRecyclerViewContainer"
        app:layout_constraintStart_toStartOf="parent">

        <EditText
            android:id="@+id/inputMsgetv"
            android:layout_width="0dp"
            android:layout_weight="4"
            android:hint="Enter message"
            android:layout_height="wrap_content"/>
        <Button
            android:id="@+id/sendMsgbtn"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:text="Send"
            android:layout_height="wrap_content" />

    </LinearLayout>

</android.support.constraint.ConstraintLayout>